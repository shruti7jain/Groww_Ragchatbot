# ──────────────────────────────────────────────────────────────
# Groww Mutual Fund FAQ RAG Chatbot — Dependencies
# Install with: pip install -r requirements.txt
# Python: 3.10+
# ──────────────────────────────────────────────────────────────

# Scraping
requests==2.31.0
beautifulsoup4==4.12.3
playwright==1.44.0          # fallback for JS-rendered pages

# Text Processing & RAG
langchain==0.3.0
langchain-community==0.3.0
sentence-transformers==3.0.1
FlagEmbedding==1.2.10       # BAAI BGE embedding model support

# Vector Store
faiss-cpu==1.9.0.post1
chromadb==0.5.3             # optional persistent alternative

# LLM (all free-tier APIs)
google-generativeai==0.5.4  # Gemini Flash (primary, free tier)
groq==0.9.0                 # Groq API fallback (free, llama3-8b-8192)

# UI
fastapi==0.111.0
uvicorn==0.29.0

# Utilities
python-dotenv==1.0.1
tqdm==4.66.4
